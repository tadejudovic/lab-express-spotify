<h1>Search Results</h1>

{{#each artist}}
<h1>{{name}}</h1>
<img src="{{ images.0.url }}" alt="artist">
<img src="{{tracks.album.images.url}}" alt="" srcset="">

<a href="/albums/{{id}}">
    <button>View Albums</button>
</a>

{{/each}}